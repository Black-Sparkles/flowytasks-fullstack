const date=v=>v?new Intl.DateTimeFormat('en-CA',{month:'short',day:'numeric',year:'numeric'}).format(new Date(v+'T00:00:00')):'No due date';
export default function TaskCard({task,onToggle,onEdit,onDelete}){return <article className={'task-card '+(task.completed?'done':'')}><div className="task-card-top"><span className={'priority '+task.priority.toLowerCase()}>{task.priority}</span><span className="due">{date(task.dueDate)}</span></div><h3>{task.title}</h3><p className="description">{task.description||'No description provided.'}</p><div className="task-actions"><button className="secondary" onClick={()=>onToggle(task.id)}>{task.completed?'Mark active':'Complete'}</button><button className="text-button" onClick={()=>onEdit(task)}>Edit</button><button className="text-button danger" onClick={()=>onDelete(task.id)}>Delete</button></div></article>}
